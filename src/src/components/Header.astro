---
const nav = [
  { href: '/', label: '首页' },
  { href: '/research', label: '研究方向' },
  { href: '/people', label: '团队成员' },
  { href: '/publications', label: '研究成果' },
  { href: '/news', label: '最新进展' },
  { href: '/media', label: '相关报道' },
  { href: '/admissions', label: '招生招聘' },
  { href: '/about', label: '关于' },
];
const path = Astro.url.pathname;
---

<header class="site">
  <div class="container">
    <a class="logo" href="/">
      <img class="logo-mark" src="/logo.svg" alt="Kan Li Group 标志" width="30" height="30" />
      <span>Kan Li Group</span>
    </a>
    <nav>
      {nav.map((item) => (
        <a href={item.href} aria-current={path === item.href ? 'page' : undefined}>
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>
